import { type ReactNode } from "react";

export default function Dropdown({options, active, select}: {options: string[], active: string, select: ReactNodd}) {
    return (
        <div className="">
        </div>
    )
}